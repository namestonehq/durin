[profile.default]
src = "src"
out = "out"
libs = ["lib"]
# The verbosity level to use during tests.
verbosity = 3
solc_version = "0.8.20" 
evm_version = "london"
optimizer = true
optimizer_runs = 200

[fmt]
bracket_spacing = true
int_types = "long"
line_length = 120
multiline_func_header = "params_first"
number_underscore = "thousands"
quote_style = "double"
tab_width = 4
wrap_comments = false

[rpc_endpoints]
# Commenting out for safety — none of the contracts in this repo are meant to be deployed to mainnet
# mainnet = "https://ethereum-rpc.publicnode.com"
# sepolia = "https://ethereum-sepolia-rpc.publicnode.com"
base = "https://base-rpc.publicnode.com"
base-sepolia = "https://base-sepolia-rpc.publicnode.com"
arbitrum = "https://arbitrum-one-rpc.publicnode.com"
arbitrum-sepolia = "https://arbitrum-sepolia-rpc.publicnode.com"
optimism = "https://optimism-rpc.publicnode.com"
optimism-sepolia = "https://optimism-sepolia-rpc.publicnode.com"
scroll = "https://scroll-rpc.publicnode.com"
scroll-sepolia = "https://scroll-sepolia-rpc.publicnode.com"
linea = "https://linea-rpc.publicnode.com"
linea-sepolia = "https://linea-sepolia-rpc.publicnode.com"

[etherscan]
# Etherscan API keys are now universal, so we only need one
# https://docs.etherscan.io/contract-verification/supported-chains
# mainnet = { key = "${ETHERSCAN_API_KEY}" }
# sepolia = { key = "${ETHERSCAN_API_KEY}" }
base = { key = "${ETHERSCAN_API_KEY}", chain = 8453 }
base-sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 84532 }
arbitrum = { key = "${ETHERSCAN_API_KEY}", chain = 42161 }
arbitrum-sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 421614 }
optimism = { key = "${ETHERSCAN_API_KEY}", chain = 10 }
optimism-sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 11155420 }
scroll = { key = "${ETHERSCAN_API_KEY}", chain = 534352 }
scroll-sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 534351 }
linea = { key = "${ETHERSCAN_API_KEY}", chain = 59144 }
linea-sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 59141 }

[fuzz]
# The amount of fuzz runs to perform for each fuzz test case. 
# Higher values gives more confidence in results at the cost of testing speed.
runs = 256

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
